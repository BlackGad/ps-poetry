[pytest]
addopts = 
  --cov=src/src 
  --cov-report=xml:coverage.xml
  --junitxml=report.xml

pythonpath =
    src

testpaths =
    plugin
    modules

norecursedirs = 
    .venv
    .reports
    .poetry
    __pycache__
    .pytest_cache
    dist
    build
	htmlcov
    experiments
