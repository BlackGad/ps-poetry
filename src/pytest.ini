[pytest]
addopts = 
  --cov=src/src 
  --cov-report=xml:coverage.xml
  --junitxml=report.xml

pythonpath =
    src

testpaths =
    plugin
    sdk
    modules

norecursedirs = 
    .venv
    .reports
    .poetry
    __pycache__
    .pytest_cache
    dist
    build
	htmlcov
    experiments
